# 应用系统简介

本系统是一个基于 C++17 实现的桌面版在线判题系统（Online Judge，简称 OJ），主要用于编程学习、算法训练和课程实验评测。系统面向普通用户和管理员两类角色：普通用户可以注册登录、浏览题目、编辑并提交代码、查看评测结果和历史提交详情；管理员可以登录后台，通过图形化表单添加题目并维护题库。系统 v2.0 使用 ImGui 构建桌面图形界面，并结合 MySQL、Docker 沙箱和 AI 辅助模块，形成从题目管理、代码提交、自动评测到结果反馈的完整闭环。

## 系统需求

从用户使用角度看，系统需要提供完整的在线评测基本流程。普通用户应能够完成注册、登录、修改密码、浏览题目、查看题目详情、编辑代码、提交评测以及查看历史提交等操作。系统还需要记录用户的提交次数、通过题目数和最近登录时间，使用户的练习过程能够被持续追踪。

从题目与评测角度看，系统需要支持题目列表展示、题目搜索、题目详情查看和管理员添加题目。用户提交代码后，系统应根据题目的时间限制、内存限制和测试数据，对代码进行编译、运行和输出比对，并返回 AC、WA、TLE、MLE、RE、CE、SE 等评测状态。评测结果不仅要即时显示，还需要写入数据库，便于用户后续查看提交详情。

从辅助学习角度看，系统需要保存用户提交记录、提交代码和评测状态，并在提交详情中展示错误原因说明。系统还可选接入 DeepSeek API，根据题目信息、用户代码和最近一次评测错误上下文，为用户提供调试建议和学习辅助。

## 系统约束

在技术实现上，系统主程序使用 C++17 开发，图形界面依赖 ImGui、GLFW 和 OpenGL，数据库使用 MySQL，评测沙箱依赖 Docker。因此，系统运行环境需要具备相应的图形显示、数据库连接和 Docker 执行能力。推荐使用 Docker Compose 启动数据库和主程序；如果在容器中运行 GUI，还需要宿主机支持 X11 图形显示。

在安全设计上，用户提交的代码不能直接在宿主机运行，必须放入受限 Docker 沙箱中执行。评测容器需要禁用网络、限制内存和进程数量、使用只读根文件系统，并通过 tmpfs 提供临时可写目录，以降低恶意代码对宿主机和系统服务的影响。

在数据与功能范围上，题目测试数据需要按 `data/<problem_id>/` 目录组织，并以 `1.in/1.out`、`2.in/2.out` 等形式成对出现。当前版本主要支持 C++17 代码评测，暂未实现多语言评测、Web 访问、异步评测和完整的逐测试点报告持久化。AI 功能依赖本地 `ai/.env` 中配置的 API Key，该文件只能作为本地配置存在，不能提交到版本库。

总体而言，本系统定位为一个教学型、实验型 OJ 平台，重点展示图形界面、数据库持久化、Docker 安全沙箱、自动评测和 AI 辅助调试等模块的协同设计。当前版本适合本地部署和课程项目演示，后续可继续扩展为支持多语言、异步评测、Web 前端和分布式评测的完整在线评测平台。

# 2 系统设计

## 2.1 总体设计

本系统采用分层模块化设计，将图形界面、业务逻辑、数据访问、评测执行和外部服务调用进行拆分。系统入口由 `main.cpp` 完成 GLFW、OpenGL 和 ImGui 的初始化，随后进入 GUI 主循环。`ViewManager` 作为界面状态控制器，根据当前 `GUIState` 分发到用户界面或管理员界面。用户侧由 `UserView` 和 `User` 共同完成登录注册、题目浏览、代码提交和提交记录查看；管理员侧由 `AdminView` 和 `Admin` 完成管理员登录和题目添加。底层通过 `DatabaseManager` 访问 MySQL，通过 `JudgeCore`、`ContainerPool` 和 `SandboxContainer` 完成 Docker 沙箱评测，通过 `AIClient` 调用 Python AI 服务。

```text
                         ┌────────────────────┐
                         │      main.cpp       │
                         │ ImGui/GLFW/OpenGL  │
                         └─────────┬──────────┘
                                   │
                                   ▼
                         ┌────────────────────┐
                         │    ViewManager     │
                         │   GUI 状态机控制    │
                         └──────┬───────┬─────┘
                                │       │
                    ┌───────────┘       └───────────┐
                    ▼                               ▼
             ┌──────────────┐               ┌──────────────┐
             │  AdminView   │               │   UserView   │
             │ 管理员界面层  │               │  用户界面层   │
             └──────┬───────┘               └──────┬───────┘
                    ▼                              ▼
             ┌──────────────┐               ┌──────────────┐
             │    Admin     │               │     User     │
             │ 管理员业务层  │               │  用户业务层   │
             └──────┬───────┘               └───┬────┬─────┘
                    │                           │    │
                    ▼                           ▼    ▼
             ┌──────────────┐          ┌────────────┐ ┌────────────┐
             │DatabaseManager│         │ JudgeCore  │ │  AIClient  │
             │  MySQL访问层  │         │ 评测核心层  │ │  AI调用层   │
             └──────────────┘          └──────┬─────┘ └──────┬─────┘
                                              ▼              ▼
                                      ┌──────────────┐ ┌──────────────┐
                                      │ContainerPool │ │ ai_service.py│
                                      │  容器池调度   │ │ DeepSeek API │
                                      └──────┬───────┘ └──────────────┘
                                             ▼
                                      ┌──────────────┐
                                      │SandboxContainer│
                                      │ Docker评测沙箱 │
                                      └──────────────┘
```

图2-1 系统功能模块结构图

系统主要划分为界面控制模块、用户功能模块、管理员功能模块、数据库模块、评测模块、沙箱容器模块和 AI 辅助模块。界面控制模块负责系统主窗口和页面切换；用户功能模块负责普通用户的认证、题目浏览、代码提交和提交记录查看；管理员功能模块负责后台登录和题目添加；数据库模块负责用户、题目和提交记录的持久化；评测模块负责代码编译、运行、测试点比对和结果生成；沙箱容器模块负责为用户代码提供安全隔离的执行环境；AI 辅助模块负责根据题目、代码和错误信息生成调试建议。

## 2.2 详细设计

系统详细设计围绕数据组织和核心类接口展开。数据组织方面，系统以 MySQL 数据表保存用户、题目和提交记录，以本地 `data/` 目录保存题目测试数据，以 `workspace/` 目录保存用户当前代码文件。类设计方面，系统将界面、业务、数据库、评测、容器和 AI 功能拆分为多个类，使各部分职责相对独立，便于维护和扩展。

## 2.2.1 数据结构设计

系统的数据主要由数据库数据、文件系统数据和内存状态数据三部分组成。数据库数据用于长期保存用户、题目和提交记录，是系统持久化的核心；文件系统数据用于保存测试用例和用户工作区代码；内存状态数据用于 GUI 运行期间的页面切换和临时显示。

数据库中主要包含 `users`、`problems` 和 `submissions` 三张表。`users` 表保存用户账号、密码哈希、角色、提交次数、通过题目数和最近登录时间等信息，用于用户认证和学习记录统计。`problems` 表保存题目编号、标题、描述、知识点、时间限制、内存限制和测试数据路径，用于题目展示和评测配置读取。`submissions` 表保存提交编号、用户编号、题目编号、提交代码、编程语言、评测状态和提交时间，用于提交历史查询和提交详情展示。

文件系统中的 `data/` 目录按题目编号组织测试数据，例如 `data/1/1.in` 和 `data/1/1.out` 表示第 1 题的第 1 组输入输出文件。评测核心会从 `1.in/1.out` 开始顺序读取测试点，直到遇到缺失文件为止。`workspace/` 目录用于保存用户当前编辑的代码，v2.0 中每个用户拥有独立的 `workspace/<user_id>/solution.cpp`，避免不同用户之间互相覆盖。

GUI 运行期间使用 `AppState` 保存界面状态和临时数据。该结构中包含当前 `GUIState`、登录输入框、题目列表、提交列表、当前题目、当前提交、代码编辑缓冲区、最近评测报告、AI 聊天记录和已完成题目集合等内容。通过集中保存这些状态，界面层可以在不同页面之间传递数据，并保证 ImGui 每帧渲染时能够获得当前页面所需的信息。

评测相关的数据结构包括 `JudgeConfig`、`TestCaseResult` 和 `JudgeReport`。`JudgeConfig` 用于描述评测限制，包括时间限制、内存限制、输出限制和语言类型；`TestCaseResult` 用于描述单个测试点的结果，包括测试点编号、状态、运行时间、内存占用和输出差异；`JudgeReport` 用于描述一次提交的完整评测结果，包括最终状态、总耗时、内存占用、错误信息、通过测试点数和测试点详情。

## 2.2.2 主要类设计

`ViewManager` 是系统 GUI 的主控制器，负责保存全局 `AppState`，并在每一帧根据 `GUIState` 调用对应的页面绘制函数。它不直接处理具体业务，而是负责主菜单、用户页面和管理员页面之间的调度。该类的核心接口是 `render()`，系统主循环会持续调用该函数完成界面更新。

`UserView` 是普通用户界面类，负责绘制用户登录、注册、用户菜单、题目列表、题目详情、提交结果、提交列表、提交详情、修改密码和 AI 助手等页面。它主要处理界面输入和按钮事件，并在需要执行业务操作时调用 `User` 类。其设计重点是将用户交互流程图形化，使用户可以在窗口中完成选题、写代码、提交和查看结果。

`AdminView` 是管理员界面类，负责管理员登录、管理员菜单和添加题目页面。v2.0 中管理员添加题目采用表单方式，管理员只需要填写标题、知识点、时间限制、内存限制、测试数据路径和题目描述，系统会将数据写入 `problems` 表。相比直接输入 SQL，该方式降低了使用门槛和误操作风险。

`User` 是普通用户业务类，封装用户登录、注册、修改密码、代码提交和提交记录处理等业务逻辑。登录和注册时，系统对用户输入进行转义，并使用 SHA256 哈希校验密码。提交代码时，`User` 会读取题目配置，调用 `JudgeCore` 完成评测，将结果写入 `submissions` 表，并更新用户的提交次数和通过题目数。

`Admin` 是管理员业务类，主要封装管理员相关的数据库操作，例如添加题目、查询题目列表和查看题目详情。管理员功能与普通用户功能分离，使系统在角色权限和功能维护上更加清晰。

`DatabaseManager` 是数据库访问类，封装 MySQL C API，提供连接数据库、执行 SQL、查询结果和字符串转义等功能。其主要接口包括 `run_sql()`、`query()` 和 `escape_string()`。其中 `query()` 返回 `vector<map<string,string>>`，便于界面层和业务层按列名读取查询结果。

`JudgeCore` 是评测核心类，采用 PIMPL 模式隐藏具体实现。外部通过 `setConfig()` 设置评测限制，通过 `setSourceCode()` 设置用户源码，通过 `setTestDataPath()` 设置测试数据路径，最后调用 `judge()` 执行评测。评测流程包括获取容器、写入源码、编译程序、加载测试数据、逐测试点运行、输出比对和生成评测报告。

`ContainerPool` 是容器池管理类，负责维护常驻容器和临时容器。评测开始时，系统优先分配空闲常驻容器；当常驻容器忙碌且未达到最大容器数量时，系统创建临时容器；评测完成后，常驻容器会被重置并归还池中，临时容器则会被销毁。该设计可以降低 Docker 冷启动带来的性能开销。

`SandboxContainer` 是单个评测容器的封装类，负责 Docker 容器的启动、源码写入、编译、运行、重置和销毁。容器启动时使用禁网、内存限制、进程数限制、只读根文件系统和 tmpfs 等参数，为用户代码提供相对安全的执行环境。

`AIClient` 是 C++ 侧 AI 调用类，负责检查 AI 服务是否可用，并通过子进程调用 `ai/ai_service.py`。调用时可以传入用户问题、代码上下文和题目信息，Python 服务读取本地 `DEEPSEEK_API_KEY` 后调用 DeepSeek API，并将回答通过标准输出返回给 C++ 程序。

从整体流程看，用户提交代码后，`UserView` 接收界面操作并调用 `User::submit_code()`；`User` 查询数据库并组织评测配置；`JudgeCore` 调用 `ContainerPool` 获取容器；`SandboxContainer` 在 Docker 沙箱中编译运行代码；评测完成后 `JudgeReport` 返回给 `User`，再写入数据库并由 GUI 展示给用户。该流程将界面交互、业务处理、评测执行和数据持久化分离，使系统结构清晰，便于后续扩展。
