# 代码评测系统

<cite>
**本文档引用的文件**
- [judge_core.h](file://include/judge_core.h)
- [judge_implementation_plan.md](file://docs/judge_implementation_plan.md)
- [code_submission_design.md](file://docs/code_submission_design.md)
- [main.cpp](file://src/main.cpp)
- [db_manager.h](file://include/db_manager.h)
- [db_manager.cpp](file://src/db_manager.cpp)
- [user.h](file://include/user.h)
- [user.cpp](file://src/user.cpp)
- [CMakeLists.txt](file://CMakeLists.txt)
- [setup.sh](file://setup.sh)
- [init.sql](file://init.sql)
- [README.md](file://README.md)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)
10. [附录](#附录)

## 简介

本项目是一个基于 Docker 容器化的在线判题系统（OJ），专注于提供安全、可靠的代码评测服务。系统采用现代化的 C++17 技术栈，结合 Docker 容器技术实现代码的安全隔离和资源限制，为用户提供高质量的编程练习和竞赛体验。

系统的核心设计理念包括：
- **安全性**：通过 Docker 容器、Seccomp 过滤和能力降级实现多层安全防护
- **可靠性**：容器池管理、资源监控和异常恢复机制确保系统稳定运行
- **可扩展性**：模块化设计支持插件化评测策略和语言扩展
- **性能优化**：容器预热、资源限制和并行评测提升系统吞吐量

## 项目结构

OJ 系统采用清晰的分层架构，主要包含以下核心目录和文件：

```mermaid
graph TB
subgraph "项目根目录"
Root[项目根目录]
Docs[docs/ - 文档目录]
Include[include/ - 头文件]
Src[src/ - 源代码]
Workspace[workspace/ - 工作区]
History[history/ - 历史记录]
AI[ai/ - AI助手]
end
subgraph "核心组件"
JudgeCore[JudgeCore - 评测核心]
DBManager[DatabaseManager - 数据库管理]
UserManager[User - 用户管理]
ViewManager[ViewManager - 视图管理]
end
Root --> Docs
Root --> Include
Root --> Src
Root --> Workspace
Root --> History
Root --> AI
Include --> JudgeCore
Include --> DBManager
Include --> UserManager
Src --> ViewManager
Src --> DBManager
Src --> UserManager
```

**图表来源**
- [CMakeLists.txt:1-40](file://CMakeLists.txt#L1-L40)
- [main.cpp:1-14](file://src/main.cpp#L1-L14)

**章节来源**
- [CMakeLists.txt:1-40](file://CMakeLists.txt#L1-L40)
- [README.md:1-2](file://README.md#L1-L2)

## 核心组件

### JudgeCore 评测核心

JudgeCore 是系统的核心接口类，负责封装完整的评测流程，包括代码编译、沙箱执行、结果判定和性能监控。

```mermaid
classDiagram
class JudgeCore {
-JudgeConfig config_
-string source_code_
-string test_data_path_
-string work_dir_
-JudgeReport last_report_
+JudgeCore()
+~JudgeCore()
+setConfig(JudgeConfig)
+setSourceCode(string)
+setTestDataPath(string)
+setWorkDirectory(string)
+judge() JudgeReport
+getLastReport() JudgeReport
+cleanup() void
}
class JudgeConfig {
+int time_limit_ms
+int memory_limit_mb
+int output_limit_mb
+int stack_limit_mb
+string language
}
class JudgeReport {
+JudgeResult result
+int time_used_ms
+int memory_used_mb
+string error_message
+int passed_test_cases
+int total_test_cases
}
class JudgeResult {
<<enumeration>>
PENDING
COMPILING
RUNNING
ACCEPTED
WRONG_ANSWER
TIME_LIMIT_EXCEEDED
MEMORY_LIMIT_EXCEEDED
RUNTIME_ERROR
COMPILE_ERROR
SYSTEM_ERROR
}
JudgeCore --> JudgeConfig : "使用"
JudgeCore --> JudgeReport : "生成"
JudgeReport --> JudgeResult : "包含"
```

**图表来源**
- [judge_core.h:53-117](file://include/judge_core.h#L53-L117)

### 数据库管理系统

DatabaseManager 提供统一的数据库访问接口，封装 MySQL 连接管理和 SQL 执行逻辑。

```mermaid
classDiagram
class DatabaseManager {
-MYSQL* conn
+DatabaseManager(host, user, password, dbname)
+~DatabaseManager()
+get_connection() MYSQL*
+run_sql(sql) bool
+query(sql) vector~map~string,string~~
}
class User {
-DatabaseManager* db_manager
-int current_user_id
-string current_account
-bool logged_in
+login(account, password) bool
+register_user(account, password) bool
+change_my_password(old, new) bool
+list_problems() void
+view_problem(id) void
+submit_code(problem_id, code, language) void
+view_my_submissions() void
+is_logged_in() bool
+get_current_user_id() int
+get_current_account() string
}
DatabaseManager <-- User : "依赖"
```

**图表来源**
- [db_manager.h:12-46](file://include/db_manager.h#L12-L46)
- [user.h:10-86](file://include/user.h#L10-L86)

**章节来源**
- [judge_core.h:1-120](file://include/judge_core.h#L1-L120)
- [db_manager.h:1-53](file://include/db_manager.h#L1-L53)
- [user.h:1-89](file://include/user.h#L1-L89)

## 架构概览

系统采用分层架构设计，通过 Docker 容器实现代码评测的完全隔离和安全执行。

```mermaid
graph TB
subgraph "应用层"
Main[main.cpp - 应用入口]
ViewManager[ViewManager - 视图管理]
User[User - 用户管理]
end
subgraph "业务逻辑层"
JudgeCore[JudgeCore - 评测核心]
DBManager[DatabaseManager - 数据库管理]
end
subgraph "容器化执行层"
DockerManager[DockerManager - 容器管理]
ContainerPool[ContainerPool - 容器池]
ResourceMonitor[ResourceMonitor - 资源监控]
end
subgraph "Docker 容器层"
Sandbox[Sandbox - 沙箱环境]
Container[Container - 评测容器]
end
subgraph "基础设施层"
DockerEngine[Docker Engine]
HostSystem[宿主机系统]
end
Main --> ViewManager
ViewManager --> User
User --> DBManager
User --> JudgeCore
JudgeCore --> DockerManager
JudgeCore --> ContainerPool
JudgeCore --> ResourceMonitor
DockerManager --> Container
ContainerPool --> Container
ResourceMonitor --> Container
Container --> Sandbox
Sandbox --> DockerEngine
DockerEngine --> HostSystem
```

**图表来源**
- [judge_implementation_plan.md:13-41](file://docs/judge_implementation_plan.md#L13-L41)
- [main.cpp:5-10](file://src/main.cpp#L5-L10)

### Docker 容器化架构

系统采用 Docker 容器技术实现代码评测的完全隔离，每个评测任务都在独立的容器环境中执行。

```mermaid
sequenceDiagram
participant Client as "客户端"
participant JudgeCore as "JudgeCore"
participant ContainerPool as "容器池"
participant DockerManager as "Docker管理器"
participant Container as "评测容器"
participant Sandbox as "沙箱环境"
Client->>JudgeCore : 提交代码评测请求
JudgeCore->>ContainerPool : 获取可用容器
ContainerPool->>DockerManager : 创建/获取容器
DockerManager->>Container : 启动容器
Container->>Sandbox : 进入沙箱环境
JudgeCore->>Sandbox : 写入源代码文件
JudgeCore->>Sandbox : 编译代码
Sandbox-->>JudgeCore : 编译结果
alt 编译成功
JudgeCore->>Sandbox : 运行测试用例
Sandbox->>Sandbox : 执行用户代码
Sandbox-->>JudgeCore : 测试结果
else 编译失败
Sandbox-->>JudgeCore : 编译错误
end
JudgeCore->>ContainerPool : 释放容器
ContainerPool->>DockerManager : 停止容器
DockerManager->>Container : 停止容器
JudgeCore-->>Client : 返回评测结果
```

**图表来源**
- [judge_implementation_plan.md:397-440](file://docs/judge_implementation_plan.md#L397-L440)

## 详细组件分析

### JudgeCore 接口设计

JudgeCore 采用现代 C++ 设计模式，提供简洁而强大的接口来管理整个评测流程。

#### 核心接口分析

```mermaid
flowchart TD
Start([开始评测]) --> SetConfig["设置评测配置"]
SetConfig --> SetSource["设置源代码"]
SetSource --> SetTest["设置测试数据路径"]
SetTest --> SetWorkDir["设置工作目录"]
SetWorkDir --> AcquireContainer["获取容器"]
AcquireContainer --> PrepareEnv["准备评测环境"]
PrepareEnv --> Compile["编译代码"]
Compile --> CompileSuccess{"编译成功?"}
CompileSuccess --> |否| CreateReportCE["创建编译错误报告"]
CreateReportCE --> ReleaseContainer["释放容器"]
ReleaseContainer --> End([结束])
CompileSuccess --> |是| RunTests["运行测试用例"]
RunTests --> AggregateResults["汇总评测结果"]
AggregateResults --> ReleaseContainer
ReleaseContainer --> End
```

**图表来源**
- [judge_implementation_plan.md:532-553](file://docs/judge_implementation_plan.md#L532-L553)

#### 评测配置管理

评测配置通过 `JudgeConfig` 结构体统一管理，支持多种编程语言和资源限制参数。

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| time_limit_ms | int | 1000 | 时间限制（毫秒） |
| memory_limit_mb | int | 128 | 内存限制（MB） |
| output_limit_mb | int | 64 | 输出限制（MB） |
| stack_limit_mb | int | 32 | 栈空间限制（MB） |
| language | string | "cpp" | 编程语言 |

#### 评测结果处理

评测结果通过 `JudgeReport` 结构体标准化，包含详细的评测信息和统计数据。

| 结果字段 | 类型 | 说明 |
|----------|------|------|
| result | JudgeResult | 评测最终结果 |
| time_used_ms | int | 实际使用时间（毫秒） |
| memory_used_mb | int | 实际使用内存（MB） |
| error_message | string | 错误信息 |
| passed_test_cases | int | 通过的测试点数量 |
| total_test_cases | int | 总测试点数量 |

**章节来源**
- [judge_core.h:26-46](file://include/judge_core.h#L26-L46)
- [judge_core.h:9-21](file://include/judge_core.h#L9-L21)

### Docker 容器化评测环境

系统采用 Docker 容器技术实现代码评测的完全隔离和安全执行。

#### 容器镜像设计

Docker 镜像采用最小化设计原则，仅包含必要的编译和运行时环境。

```mermaid
graph LR
subgraph "基础镜像"
Ubuntu[ubuntu:22.04]
end
subgraph "编译环境"
GCC[gcc/g++]
Make[make]
Time[time命令]
end
subgraph "安全配置"
RunnerUser[runner用户]
Seccomp[Seccomp配置]
CapDrop[能力降级]
end
subgraph "工作目录"
Sandbox[/sandbox]
Script[sandbox_runner.sh]
end
Ubuntu --> GCC
Ubuntu --> Make
Ubuntu --> Time
Ubuntu --> RunnerUser
RunnerUser --> Seccomp
RunnerUser --> CapDrop
Sandbox --> Script
```

**图表来源**
- [judge_implementation_plan.md:61-87](file://docs/judge_implementation_plan.md#L61-L87)

#### 沙箱启动脚本

沙箱启动脚本负责在容器内执行完整的编译和运行流程，确保评测过程的标准化。

```mermaid
flowchart TD
Start([容器启动]) --> SetPaths["设置文件路径"]
SetPaths --> WriteSource["写入源代码"]
WriteSource --> Compile["编译代码"]
Compile --> CompileOK{"编译成功?"}
CompileOK --> |否| ReturnCE["返回编译错误"]
CompileOK --> |是| RunWithTimeout["运行程序带超时"]
RunWithTimeout --> CheckExitCode{"退出码检查"}
CheckExitCode --> |124| ReturnTLE["返回超时"]
CheckExitCode --> |-1| ReturnRE["返回运行时错误"]
CheckExitCode --> |0| ReturnSuccess["返回成功"]
ReturnCE --> End([结束])
ReturnTLE --> End
ReturnRE --> End
ReturnSuccess --> End
```

**图表来源**
- [judge_implementation_plan.md:91-125](file://docs/judge_implementation_plan.md#L91-L125)

### 容器池管理系统

容器池管理器负责容器的生命周期管理、资源分配和负载均衡。

#### 容器池设计

```mermaid
classDiagram
class ContainerPool {
-queue~shared_ptr~Container~~ available_
-vector~shared_ptr~Container~~ all_containers_
-mutex mutex_
-condition_variable cv_
-int min_size_
-int max_size_
+initialize(min_size, max_size) void
+acquire() shared_ptr~Container~
+release(container) void
+expand(count) void
+healthCheck() void
}
class Container {
<<enumeration>> State {
IDLE
RUNNING
BUSY
ERROR
}
-string container_id_
-State state_
-steady_clock : : time_point last_used_
+create(config) bool
+start() bool
+stop() bool
+destroy() bool
+reset() bool
+getState() State
+getId() string
}
ContainerPool --> Container : "管理多个"
```

**图表来源**
- [judge_implementation_plan.md:132-158](file://docs/judge_implementation_plan.md#L132-L158)

#### 并行评测流程

系统支持多容器并行评测，通过容器池实现动态负载均衡。

```mermaid
sequenceDiagram
participant Queue as "评测请求队列"
participant Scheduler as "调度器"
participant Pool as "容器池"
participant Container1 as "容器1"
participant Container2 as "容器2"
participant ContainerN as "容器N"
Queue->>Scheduler : 新评测请求
Scheduler->>Pool : 请求可用容器
Pool->>Container1 : 分配容器1
Pool->>Container2 : 分配容器2
Pool->>ContainerN : 分配容器N
Scheduler->>Container1 : 执行评测1
Scheduler->>Container2 : 执行评测2
Scheduler->>ContainerN : 执行评测N
Container1-->>Scheduler : 评测1完成
Container2-->>Scheduler : 评测2完成
ContainerN-->>Scheduler : 评测N完成
Scheduler->>Pool : 归还容器
Pool->>Container1 : 释放容器1
Pool->>Container2 : 释放容器2
Pool->>ContainerN : 释放容器N
```

**图表来源**
- [judge_implementation_plan.md:162-179](file://docs/judge_implementation_plan.md#L162-L179)

### 安全权限控制系统

系统采用多层安全防护机制，确保评测过程的安全性和隔离性。

#### Docker 安全配置

```mermaid
graph TB
subgraph "Docker 安全选项"
DisableNetwork[禁用网络访问]
ReadOnlyFS[只读文件系统]
NoPrivileged[禁止特权模式]
CapDrop[丢弃所有 capabilities]
CapAdd[添加最小必要 capabilities]
Seccomp[Seccomp 配置文件]
AppArmor[AppArmor 配置文件]
end
subgraph "资源限制"
MaxPIDs[最大进程数: 50]
MaxOpenFiles[最大打开文件数: 64]
end
DisableNetwork --> CapDrop
ReadOnlyFS --> CapDrop
NoPrivileged --> CapDrop
CapDrop --> Seccomp
CapDrop --> AppArmor
Seccomp --> MaxPIDs
AppArmor --> MaxOpenFiles
```

**图表来源**
- [judge_implementation_plan.md:225-252](file://docs/judge_implementation_plan.md#L225-L252)

#### Seccomp 系统调用过滤

系统使用 Seccomp（Secure Computing Mode）技术限制容器内的系统调用，只允许必要的系统调用。

| 允许的系统调用 | 作用 |
|----------------|------|
| read, write | 文件读写操作 |
| open, close | 文件句柄管理 |
| mmap, mprotect, munmap | 内存映射管理 |
| brk | 内存分配控制 |
| exit, exit_group | 进程退出 |
| execve | 程序执行 |
| arch_prctl | 架构控制 |

**章节来源**
- [judge_implementation_plan.md:256-280](file://docs/judge_implementation_plan.md#L256-L280)

### 资源监控与限制

系统实现精确的资源监控和限制机制，确保评测过程的公平性和稳定性。

#### 资源限制配置

```mermaid
classDiagram
class ResourceLimits {
+float cpu_quota 1.0
+int cpu_period 100000
+int memory_limit_mb
+int memory_swap_mb 0
+int time_limit_ms
+int wall_time_limit_ms
+int output_limit_mb 64
+int max_processes 1
}
class ResourceUsage {
+int64_t memory_usage_bytes
+int64_t memory_peak_bytes
+float cpu_percent
+int64_t cpu_time_us
+bool oom_killed
}
class ResourceMonitor {
+startMonitoring(container_id) void
+getUsage() ResourceUsage
+checkLimits(limits) bool
+stopMonitoring() void
-parseStats(stats_json) ResourceUsage
-calculateMemoryUsage(json) int64_t
-calculateCPUUsage(json) float
}
ResourceMonitor --> ResourceLimits : "使用"
ResourceMonitor --> ResourceUsage : "返回"
```

**图表来源**
- [judge_implementation_plan.md:318-375](file://docs/judge_implementation_plan.md#L318-L375)

#### 实时监控实现

系统通过 Docker Stats API 和 cgroup 接口实现精确的资源使用监控。

```mermaid
flowchart TD
Start([开始监控]) --> GetStats["获取容器统计信息"]
GetStats --> ParseJSON["解析 JSON 格式"]
ParseJSON --> ExtractMemory["提取内存使用数据"]
ParseJSON --> ExtractCPU["提取 CPU 使用数据"]
ParseJSON --> ExtractPIDs["提取进程数"]
ExtractMemory --> CalcMemory["计算内存使用量"]
ExtractCPU --> CalcCPU["计算 CPU 使用率"]
ExtractPIDs --> CheckLimits["检查资源限制"]
CalcMemory --> CheckLimits
CalcCPU --> CheckLimits
CheckLimits --> MemoryExceeded{"内存超限?"}
CheckLimits --> CPUExceeded{"CPU超限?"}
CheckLimits --> PIDExceeded{"进程数超限?"}
MemoryExceeded --> |是| KillProcess["终止进程"]
CPUExceeded --> |是| KillProcess
PIDExceeded --> |是| KillProcess
MemoryExceeded --> |否| Continue["继续监控"]
CPUExceeded --> |否| Continue
PIDExceeded --> |否| Continue
KillProcess --> End([结束])
Continue --> Sleep["等待间隔"]
Sleep --> GetStats
```

**图表来源**
- [judge_implementation_plan.md:342-375](file://docs/judge_implementation_plan.md#L342-L375)

**章节来源**
- [judge_implementation_plan.md:312-390](file://docs/judge_implementation_plan.md#L312-L390)

## 依赖关系分析

系统采用模块化设计，各组件之间的依赖关系清晰明确。

```mermaid
graph TB
subgraph "外部依赖"
MySQL[MySQL Client Library]
OpenSSL[OpenSSL Crypto]
Docker[Docker Engine API]
end
subgraph "内部组件"
JudgeCore[JudgeCore]
DBManager[DatabaseManager]
User[User]
ViewManager[ViewManager]
DockerManager[DockerManager]
ContainerPool[ContainerPool]
ResourceMonitor[ResourceMonitor]
end
subgraph "构建系统"
CMake[CMake Build System]
Compiler[GCC/G++]
end
CMake --> Compiler
Compiler --> JudgeCore
Compiler --> DBManager
Compiler --> User
Compiler --> ViewManager
JudgeCore --> DBManager
JudgeCore --> DockerManager
JudgeCore --> ContainerPool
JudgeCore --> ResourceMonitor
DockerManager --> Docker
DBManager --> MySQL
User --> DBManager
ViewManager --> User
JudgeCore --> OpenSSL
```

**图表来源**
- [CMakeLists.txt:11-34](file://CMakeLists.txt#L11-L34)

### 数据库设计

系统使用 MySQL 作为数据存储，采用规范化设计确保数据一致性和完整性。

```mermaid
erDiagram
USERS {
int id PK
varchar account UK
varchar password_hash
int submission_count
int solved_count
timestamp created_at
timestamp last_login
}
PROBLEMS {
int id PK
varchar title
text description
int time_limit
int memory_limit
varchar test_data_path
varchar category
timestamp created_at
}
SUBMISSIONS {
int id PK
int user_id FK
int problem_id FK
text code
enum status
timestamp submit_time
}
USERS ||--o{ SUBMISSIONS : "has"
PROBLEMS ||--o{ SUBMISSIONS : "has"
```

**图表来源**
- [init.sql:14-61](file://init.sql#L14-L61)

**章节来源**
- [CMakeLists.txt:11-34](file://CMakeLists.txt#L11-L34)
- [init.sql:14-95](file://init.sql#L14-L95)

## 性能考虑

系统在设计时充分考虑了性能优化，通过多种技术手段提升评测效率和用户体验。

### 容器预热机制

系统采用容器预热策略，在系统启动时预先创建一定数量的容器，减少评测时的等待时间。

```mermaid
flowchart TD
SystemStart([系统启动]) --> PreCreate["预创建容器"]
PreCreate --> MinContainers["创建最小容器数量"]
MinContainers --> ReadyState["容器进入就绪状态"]
ReadyState --> WaitRequests["等待评测请求"]
WaitRequests --> NewRequest{"收到新请求?"}
NewRequest --> |否| WaitRequests
NewRequest --> |是| UseExisting["使用现有容器"]
UseExisting --> ResetContainer["重置容器状态"]
ResetContainer --> ExecuteTest["执行评测"]
ExecuteTest --> ReturnContainer["归还容器"]
ReturnContainer --> ReadyState
```

**图表来源**
- [judge_implementation_plan.md:647-657](file://docs/judge_implementation_plan.md#L647-L657)

### 性能指标优化

系统设定了明确的性能目标，确保评测系统的高效运行。

| 性能指标 | 目标值 | 优化措施 |
|----------|--------|----------|
| 容器启动时间 | < 500ms | 镜像精简、预创建 |
| 编译时间 | 正常 | 缓存编译结果 |
| 单点测试 | < 时间限制 + 50ms | 精确计时 |
| 并发评测数 | 10+ | 容器池管理 |
| 内存占用 | < 200MB/容器 | Alpine Linux |

### 资源利用优化

系统通过合理的资源分配和监控，最大化利用硬件资源。

```mermaid
graph LR
subgraph "CPU 优化"
CPULimit[CPU 限制: 1.0 核心]
CPUPool[CPU 池管理]
LoadBalance[负载均衡]
end
subgraph "内存优化"
MemLimit[内存限制: 128MB]
MemPool[内存池]
SwapDisable[禁用交换分区]
end
subgraph "I/O 优化"
IOOptimize[I/O 优化]
TempFS[临时文件系统]
ReadOnly[只读挂载]
end
CPULimit --> CPUPool
CPUPool --> LoadBalance
MemLimit --> MemPool
MemPool --> SwapDisable
IOOptimize --> TempFS
IOOptimize --> ReadOnly
```

**章节来源**
- [judge_implementation_plan.md:641-685](file://docs/judge_implementation_plan.md#L641-L685)

## 故障排除指南

系统提供了完善的异常处理和错误恢复机制，确保在各种异常情况下都能保持系统的稳定运行。

### 异常分类与处理

系统将异常分为多个类别，每类异常都有相应的处理策略。

| 异常类型 | 说明 | 处理策略 |
|----------|------|----------|
| 编译错误 | 代码语法错误 | 返回 CE，记录错误信息 |
| 运行时错误 | 段错误、异常退出 | 返回 RE，记录退出码 |
| 资源超限 | TLE/MLE | 强制终止，返回对应结果 |
| 系统错误 | Docker 故障 | 销毁容器，重新创建 |
| 网络错误 | 与容器通信失败 | 重试3次，失败则标记 SE |

### 错误恢复机制

系统实现了多层次的错误恢复机制，确保系统在出现故障时能够快速恢复。

```mermaid
flowchart TD
ErrorDetected[检测到错误] --> ClassifyError["分类错误类型"]
ClassifyError --> CompileError["编译错误"]
ClassifyError --> RuntimeError["运行时错误"]
ClassifyError --> ResourceExceeded["资源超限"]
ClassifyError --> SystemError["系统错误"]
ClassifyError --> NetworkError["网络错误"]
CompileError --> ReturnCE["返回编译错误"]
RuntimeError --> ReturnRE["返回运行时错误"]
ResourceExceeded --> ForceStop["强制停止容器"]
SystemError --> RecoverContainer["容器故障恢复"]
NetworkError --> RetryOperation["重试操作"]
ForceStop --> CleanupContainer["清理容器"]
RecoverContainer --> DestroyContainer["销毁故障容器"]
DestroyContainer --> CreateNewContainer["创建新容器"]
CreateNewContainer --> ReplaceContainer["替换容器"]
ReplaceContainer --> CleanupContainer
CleanupContainer --> End([结束])
ReturnCE --> End
ReturnRE --> End
RetryOperation --> End
```

**图表来源**
- [judge_implementation_plan.md:594-637](file://docs/judge_implementation_plan.md#L594-L637)

### 调试工具和诊断

系统提供了多种调试工具和诊断方法，帮助开发者快速定位和解决问题。

#### 日志记录

系统采用分级日志记录机制，不同级别的错误和警告都有相应的日志级别。

#### 性能监控

系统内置性能监控功能，可以实时监控容器的资源使用情况。

#### 容器状态检查

系统提供容器状态检查功能，可以诊断容器的健康状况。

**章节来源**
- [judge_implementation_plan.md:591-637](file://docs/judge_implementation_plan.md#L591-L637)

## 结论

OJ 代码评测系统通过现代化的架构设计和技术实现，为用户提供了一个安全、可靠、高效的代码评测平台。系统的主要优势包括：

### 技术优势

1. **安全性**：采用 Docker 容器化技术，实现代码执行的完全隔离
2. **可靠性**：多层异常处理和错误恢复机制确保系统稳定运行
3. **可扩展性**：模块化设计支持插件化评测策略和语言扩展
4. **性能优化**：容器预热、资源限制和并行评测提升系统吞吐量

### 架构特点

1. **分层架构**：清晰的层次划分便于维护和扩展
2. **接口设计**：简洁的接口设计降低使用复杂度
3. **容器化部署**：Docker 容器实现标准化的评测环境
4. **资源监控**：精确的资源使用监控确保公平性

### 发展前景

系统为未来的功能扩展奠定了良好的基础，包括：
- 支持更多编程语言
- 实现更复杂的评测策略
- 增强 AI 辅助功能
- 优化用户体验

通过持续的改进和优化，OJ 系统将成为一个功能完善、性能优异的在线编程练习平台。

## 附录

### 配置选项

系统支持多种配置选项，可以根据实际需求进行调整。

#### 编译配置

- C++ 标准：C++17
- 编译器：GCC/G++
- 优化级别：-O2

#### Docker 配置

- 基础镜像：ubuntu:22.04
- 编译环境：g++, gcc, make, time
- 用户：runner（非特权用户）

#### 资源限制

- 时间限制：可配置（默认 1000ms）
- 内存限制：可配置（默认 128MB）
- 输出限制：可配置（默认 64MB）

### 部署指南

系统提供了一键部署脚本，简化部署流程。

```bash
# 1. 创建项目目录
mkdir -p build
mkdir -p test_data/1

# 2. 初始化数据库
mysql -u root -p < init.sql

# 3. 编译和运行
cd build
cmake ..
make
./oj_app
```

### 开发指南

#### 代码规范

- 使用 C++17 标准
- 遵循 RAII 原则
- 使用智能指针管理资源
- 实现异常安全代码

#### 测试策略

- 单元测试：覆盖核心功能
- 集成测试：验证组件协作
- 性能测试：评估系统性能
- 安全测试：验证安全机制

#### 扩展开发

系统设计支持插件化扩展，开发者可以：
- 添加新的编程语言支持
- 实现自定义评测策略
- 扩展用户界面功能
- 集成第三方服务